- name: test 34 on loops + when
  hosts: localhost
  connection: local
  become: yes

  tasks:
    - name: print the package
      ansible.builtin.debug:
        msg: "Installing {{ item }}"
          
      loop:
        - git
        - nginx
        - curl
      when: item  == "nginx"