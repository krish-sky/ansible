- name: test 32 on loops
  hosts: localhost
  connection: local
  become: yes

  tasks:
    - name: installing the package
      ansible.builtin.debug:
        name: " installing '{{ item }}'"
        state: present
      
      loop:
        - git
        - nginx
        - curl