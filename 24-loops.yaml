- name: test 37
  hosts: localhost
  connection: local
  vars:
    packages:
      - git 
      - nginx
      - curl
  tasks:
    - name: Print the nginx
      ansible.builtin.debug:
        msg: "Installing {{ packages }}"
      loop: "{{ packages }}"
      when: package == "nginx"
