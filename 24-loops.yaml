- name: test 36 loop with variable
  hosts: localhost
  connection: local
  vars:
    package: 
      - git
      - nginx
      - curl
  
  tasks: 
    - name: installing package
      ansible.builtit.debug:
        msg: "installing {{ item }}"
      loop: "{{ package }}"
