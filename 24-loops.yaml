- name: test 34 on loops + when
  hosts: localhost
  become: yes

  tasks:
    - name: installing the package
      ansible.builtin.debug:
        msg: " installing {{ item }}"
      when: "{{ item }} == 'nginx'"
        
      loop:
        - git
        - nginx
        - curl