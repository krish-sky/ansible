- name: removing nginx
  hosts: localhost
  connection: local
  become: yes
  tasks:
    - name: installing nginx package
      ansible.builtin.package:
        name: nginx
        state: present
    
    - name: removing nginx package
      ansible.builtin.package:
        name: nginx
        state: absent 