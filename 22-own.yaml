- name: Combined test
  hosts: web

  vars:
    app_name: httpd

  tasks:
    - name: Task 1
      ansible.builtin.debug:
        msg: "Installing {{ app_name }}"

    - name: Task 2
      vars:
        app_name: tomcat
      ansible.builtin.debug:
        msg: "Installing {{ app_name }}"