  - name: demo on condition
    hosts: localhost
    connection: local
    
    vars:
      env: prod
      app: web
    tasks:
      - name: checking when condition
        ansible.builtin.debug:
          msg: "Deploy allowed"
        when: env == "dev" or app == "web"
    