  - name: demo on condition
    hosts: localhost
    connection: local
    
    vars:
      install_package: false
    tasks:
      - name: run only if install_package is true
        ansible.builtin.debug:
          msg: "Installing package"
        when: install_package