- name: condition with inventory
  hosts: web
  tasks:
    - name: checking condition on when
      ansible.builtin.debug:
        msg: "Deploy only DEV WEB servers"
      when: "'web' in group_names and env == 'dev'"