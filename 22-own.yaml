- name: condition with inventory
  hosts: all
  tasks:
    - name: checking condition on when
      ansible.builtin.debug:
        msg: "This runs only on WEB group"
      when: "'web' in group_names"