- name: condition with inventory
  hosts: all
  tasks:
    - name: web configuration block
      block:
        - name: task 1
          ansible.builtin.debug:
            msg: "Installing WEB packages"
      
        - name: task 2
          ansible.builtin.debug:
            msg: "Starting WEB service"
      when: "'web' in group_names"