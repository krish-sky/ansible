- name: test facts 7
  hosts: localhost
  connection: local
  vars:
    myos: "RedHat"
  
  tasks:
    - name: {{ myos }}
      ansible.builtin.debug:
        msg: "This is my target OS"
      
      when: ansible_distribution == myos