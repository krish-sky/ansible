- name: Facts test 1
  hosts: localhost
  connection: local
  gather_facts: "newone"
  tasks:
    - name: print os name
      ansible.builtin.debug:
        msg: "{{ newone }}.ansible_distribution"
        

