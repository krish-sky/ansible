- name: test facts 2
  hosts: localhost
  connection: local
  
  tasks:
    - name: Print ansible hostname and ansible distribution
      ansible.builtin.debug:
        msg: " ansible distribution is {{ ansible_distribution }} and ansible hostname is {{ ansible_hostname }}"
