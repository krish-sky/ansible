- name: test facts 4
  hosts: localhost
  connection: local
  vars:
    myos: "RedHat"
  
  tasks:
    - name: checking os 
      ansible.builtin.debug:
        msg: "{{ myos }}"
      
      when: ansible_distribution == "myos"