- name: test facts 2
  hosts: localhost
  connection: local
  
  tasks:
    - name: checking ansible distribution
      ansible.builtin.debug:
        msg: "This is redhat Linux"
      when: ansible_distribution == "RedHat"
