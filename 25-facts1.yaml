- name: test facts 4
  hosts: localhost
  connection: local
  vars:
    myos: "RedHat"
  
  tasks:
    - name: checking os
      ansible.builtin.debug:
        msg: "System is Amazon machine"
      
      when: ansible_distribution == myos